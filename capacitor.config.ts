import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sumativafran.app',
  appName: 'sumativafran',
  webDir: 'dist/myapp/browser'
};

export default config;
